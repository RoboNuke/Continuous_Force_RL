# Pose Control Experiment Configuration 

# Base configuration to inherit from
base_config: "configs/base/PiH_base.yaml"

primary:
  ckpt_tracker_path: "/nfs/stak/users/brownhun/oct6_pose_tracker.txt"
  #break_forces: [5,10]
  
# Model configuration for hybrid agent
model:
  use_hybrid_agent: false

# Experiment metadata
experiment:
  name: "pose_noAdvNorm_pLR1-5"
  tags: []
  group: "pose_noAdvNorm_pLR1-5"