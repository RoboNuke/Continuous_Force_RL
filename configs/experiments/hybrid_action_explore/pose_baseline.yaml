
# Base configuration to inherit from
base_config: "configs/base/PiH_base.yaml"
# Experiment metadata
experiment:
  name: "pose"
  tags: []
  group: "pose"
  wandb_project: "hybrid_action_tests"

primary:
  ckpt_tracker_path: "/nfs/stak/users/brownhun/hybrid_action_test_tracker.txt"
  agents_per_break_force: 3
  break_forces: [20]
  max_steps: 5000000
  seed: 42


# Model configuration for hybrid agent
model:
  critic_output_init_mean: 30  # Higher initial value estimate for complex task

